// Core Idea:

// For each bar:

// Find nearest smaller element on left

// Find nearest smaller element on right

// Width = right - left - 1

// Area = height × width


#include <bits/stdc++.h>
using namespace std;



vector<int> area(vector<int>& arr){
    int n = arr.size();
    int max_height = 0;
    for(int i {0}; i<n; i++){
        for(int j {0}; j<n; j++){
            if(arr[i] == )
        }
    }
}

int main() {
    vector<int> ar = {2,1,5,6,2,3};
    return 0;
}