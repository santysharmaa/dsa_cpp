#include <bits/stdc++.h>
using namespace std;

bool triplet(vector<int>& x, int trg){
    

}

int main(){
    vector<int> x = {1, 4, 45, 6, 10, 8};
    int trg = 13;
    cout<<(triplet(x, trg) ? "True" : "False");
    return 0;
}